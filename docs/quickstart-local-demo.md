# Local Demo Quickstart

Phase 0 only proves the public project shape. The runnable Pulse engine arrives
in later phases.

For now, verify the skeleton:

```sh
npm test
npm run typecheck
npm run build
npm run docs:check
```

The public demo config is [../pulses.example.yaml](../pulses.example.yaml). Do
not add real obligations to that file.
